[package]
name = "aktenakrobat"        # 🔧 lowercase and hyphenated name
version = "0.1.0"
edition = "2021"             # ✅ Use 2021 for better compiler compatibility

[dependencies]
clap = { version = "4.4", features = ["derive"] }
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
